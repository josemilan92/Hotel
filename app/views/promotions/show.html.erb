<p id="notice"><%= notice %></p>
<div class="jumbotron " >
  <div class="container"  >


    <div class="row">
      <div class="col-md-4">
        <%= month_calendar events: @sendpros do |date, sendpro| %>
            <%= date.day %>
            <% sendpro.each do |meeting| %>
                <div style="background-color: red">
                  <%= meeting.lugar %>
                </div>
            <% end %>
        <% end %>
      <div class="page-header">
        <h3>Nombre:</h3>
      </div>
    <%= @promotion.name %>
      </div>
    <div class="col-md-8">
      <%= render 'sendpros/form' %>
    </div>

    <div class="col-md-2">

      <%= link_to 'Regresar', promotions_path, :class=>'btn' %>

    </div>
    </div>
    </div>
      </div>


    </div>
  </div>
</div>
