<p id="notice"><%= notice %></p>

<h1>Promociones</h1>

<table class="table table-hover">
  <thead>
  <tr>
    <th>Lugar</th>
    <th>Precio</th>
    <th>Descripción</th>
    <th>Foto</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @promotions.each do |post| %>
      <tr>
        <td><%= post.name %></td>
        <td><%= post.price %></td>
        <td><%= post.description %></td>
        <td><%= image_tag post.photo, width:100 %></td>
        <td><%= link_to 'Editar', edit_promotion_path(post), class:'btn btn-default' %></td>
        <td><%= link_to 'Eliminar', post, method: :delete, data: { confirm: 'Are you sure?' } , class:'btn btn-default'%></td>
      </tr>
  <% end %>
  </tbody>
</table>
<p><%= paginate @promotions %></p>
<br>

<%= link_to 'Nueva Promocion', new_promotion_path, class:'btn btn-default' %>

