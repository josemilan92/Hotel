<p id="notice"><%= notice %></p>


<table class="table table-hover">
  <thead>
  <tr>
    <th>Nombre</th>
    <th>Descripción</th>
    <th>Foto</th>
    <th>Precio</th>
    <th>Categoria</th>
    <th>Subcategoria</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @products.each do |product| %>
      <tr>
        <td><%= product.title %></td>
        <td><%= product.description %></td>
        <td><%= image_tag product.photo ,:class => 'img-responsive', :style =>'height:100px'%></td>
        <td><%= product.price %></td>
        <td><%= product.category %></td>
        <td><%= product.subcategory %></td>
        <td><%= link_to 'Editar', edit_product_path(product), :class=>'btn' %></td>
        <td><%= link_to 'Eliminar', product, method: :delete, data: { confirm: 'Are you sure?' }, :class=>'btn' %></td>
      </tr>

  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path %>
