<p id="notice"><%= notice %></p>

<h1>Rservación de Habitación</h1>

<table class="table table-hover">
  <thead>
  <tr>
    <th>Lugar</th>
    <th>Pago</th>
    <th>Total</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @sends.each do |post| %>
      <tr>
        <td><%= post.lugar %></td>
        <td> <%if post.estatus ==1  %>(Reservado)
          <% else %>
          (En uso)
          <%end%></td>
        <td><%= post.total %></td>
        <td><%= link_to 'Editar', edit_send_path(post), class:'btn btn-default' %></td>
        <td><%= link_to 'Eliminar', post, method: :delete, data: { confirm: 'Are you sure?' } , class:'btn btn-default'%></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<p id="notice"><%= notice %></p>

<h1>Reservacion por Promoción</h1>

<table class="table table-hover">
  <thead>
  <tr>
    <th>Lugar</th>
    <th>Pago</th>
    <th>Total</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @sendpros.each do |post| %>
      <tr>
        <td><%= post.lugar %></td>
        <td><%= post.pago %></td>
        <td><%= post.total %></td>
        <td><%= link_to 'Editar', edit_sendpro_path(post), class:'btn btn-default' %></td>
        <td><%= link_to 'Eliminar', post, method: :delete, data: { confirm: 'Are you sure?' } , class:'btn btn-default'%></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Crear Reservación', new_send_path, class:'btn btn-default' %>

